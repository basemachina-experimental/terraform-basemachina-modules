# ========================================
# AWS Configuration
# ========================================
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_DEFAULT_REGION=ap-northeast-1

# ========================================
# Network Configuration (Required)
# ========================================
TEST_VPC_ID=vpc-xxxxx
TEST_PRIVATE_SUBNET_IDS=subnet-private1,subnet-private2
TEST_PUBLIC_SUBNET_IDS=subnet-public1,subnet-public2

# ========================================
# Bridge Configuration (Required)
# ========================================
TEST_TENANT_ID=your-tenant-id

# ========================================
# Certificate Configuration (Required)
# ========================================
# Option 1: Use existing ACM certificate
# TEST_CERTIFICATE_ARN=arn:aws:acm:region:account:certificate/xxxxx

# Option 2: Generate self-signed certificate and import to ACM (recommended for testing)
TEST_ENABLE_ACM_IMPORT=true

# ========================================
# Automatic Configuration (No Changes Needed)
# ========================================
# The following settings are automatically configured:
# - Private subnets only (no public IP assignment)
# - VPC Endpoints enabled (ECR API, ECR Docker, S3, CloudWatch Logs)
# - ECR Pull-Through Cache enabled (Public ECR → Private ECR caching)
# - No NAT Gateway required
# - HTTPS only (HTTP listener disabled)
